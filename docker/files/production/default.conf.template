upstream dictaphone_backend {
    server ${BACKEND_INTERNAL_HOST}:8000 fail_timeout=0;
}

upstream dictaphone_frontend {
    server ${FRONTEND_INTERNAL_HOST}:8080 fail_timeout=0;
}

server {
    listen 8083;
    server_name localhost;
    charset utf-8;

    # Disables server version feedback on pages and in headers
    server_tokens off;

    proxy_ssl_server_name on;

    location @proxy_to_dictaphone_backend {
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_redirect off;
        proxy_pass http://dictaphone_backend;
    }

    location @proxy_to_dictaphone_frontend {
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_redirect off;
        proxy_pass http://dictaphone_frontend;
    }

    location / {
        try_files $uri @proxy_to_dictaphone_frontend;
    }

    location /api {
        try_files $uri @proxy_to_dictaphone_backend;
    }

    location /admin {
        try_files $uri @proxy_to_dictaphone_backend;
    }

    location /static {
        try_files $uri @proxy_to_dictaphone_backend;
    }
}